import { ModelInstance, Server } from 'ember-cli-mirage';
import User from 'ember-osf-web/models/user';

/* eslint-disable-next-line */
export function cedarScenario(
    /* eslint-disable-next-line */
    server: Server,
    /* eslint-disable-next-line */
    currentUser: ModelInstance<User>,
/* eslint-disable-next-line */
) { }
