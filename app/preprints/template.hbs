{{page-title this.theme.provider.providerTitle replace=true}}
{{#if this.theme.isProvider}}
    <div {{with-branding (get-model this.theme.provider.brand)}}>
        <ThemeStyles />
        <BrandedNavbar
            @brandRoute='preprints.discover'
        />
    </div>
{{/if}}
{{outlet}}

<div>
    <Preprints::-Components::BrandedFooter
        @footerLinks={{this.model.provider.footerLinks}}
    ></Preprints::-Components::BrandedFooter>
</div>