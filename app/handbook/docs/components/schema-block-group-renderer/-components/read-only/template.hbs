<DocsDemo as |demo|>
    <demo.example @name='schema-block-group-renderer.read-only.hbs'>
        {{#each @schemaBlockGroups as |group|}}
            <Registries::SchemaBlockGroupRenderer
                @schemaBlockGroup={{group}}
                @renderStrategy={{
                    component
                    'registries/schema-block-renderer/read-only/mapper'
                    registrationResponses=@registrationResponses
                    node=@node
                }}
            />
        {{/each}}
    </demo.example>

    <demo.snippet @name='schema-block-group-renderer.read-only.hbs' />
</DocsDemo>
