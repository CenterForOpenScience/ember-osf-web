<DocsDemo as |demo|>
    <demo.example @name='schema-block-group-renderer.editable.hbs'>
        {{#each @schemaBlockGroups as |group|}}
            <Registries::SchemaBlockGroupRenderer
                @schemaBlockGroup={{group}}
                @renderStrategy={{
                    component
                    'registries/schema-block-renderer/editable/mapper'
                    changeset=@changeset
                    node=@node
                }}
            />
        {{/each}}
    </demo.example>

    <demo.snippet @name='schema-block-group-renderer.editable.hbs' />
</DocsDemo>
