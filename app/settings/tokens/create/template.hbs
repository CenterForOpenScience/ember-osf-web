<div>
    <OsfLink
        data-test-back-to-tokens
        @route='settings.tokens'
        @onClick={{action 'click' 'link' 'Settings - Tokens - Create - Back to list' target=this.analytics}}
    >
        {{fa-icon 'arrow-left'}}
        {{t 'settings.tokens.backToList'}}
    </OsfLink>
</div>
<h4>{{t 'settings.tokens.createToken'}}</h4>

{{#settings/tokens/-components/token-form
    onSave=(action 'onSave')
    analyticsScope='Settings - Tokens - Create'
    as |form|
}}
    <OsfButton
        data-test-create-token-button
        @type='primary'
        @buttonType='submit'
        @disabled={{form.disabled}}
    >
        {{t 'settings.tokens.createToken'}}
    </OsfButton>
{{/settings/tokens/-components/token-form}}