{{#unless this.model.taskInstance.isRunning}}
    <NodeNavbar @node={{this.model.taskInstance.value}} />
{{/unless}}

<div data-analytics-scope='Registration' local-class='Registrations__watermarked'>
    <div class='container' local-class='alert alert-info'>
        {{t 'registration.project_alert' projectId=this.projectId htmlSafe=true}}
    </div>
    {{outlet}}
</div>
