{{! template-lint-disable invalid-interactive }}
{{#bs-modal onHidden=(action 'closeModal') as |modal|}}
    {{#if (not done)}}
        {{#modal.header onClose=(action 'closeModal')}}
            <h3 class="modal-title">{{t 'new_project.header'}}</h3>
        {{/modal.header}}
        {{#modal.body}}
            <div class="text-left">
                <div class="form-group m-v-sm">
                    <label for="&quot;projectName" class="f-w-lg text-bigger">{{t 'general.title'}}</label>
                    {{input placeholder=(t 'new_project.title_placeholder') class='form-control project-name' value=nodeTitle type="text"}}
                </div>
                {{#if user.institutions.length}}
                    <div class="form-group m-v-sm">
                        <label class="f-w-lg text-bigger">{{t 'new_project.affiliation'}}</label>
                        <a class='pointer' style="float: right;" onclick={{action 'removeAll'}}>{{t 'new_project.remove_all'}}</a>
                        <a class='pointer' style="float: right; margin-right: 12px;" onclick={{action 'selectAll'}}>{{t 'new_project.select_all'}}</a>
                        <table>
                            <tr>
                                {{#each user.institutions as | institution | }}
                                    <td>
                                        <a onclick={{action 'select' institution}}>
                                            <div width="45px" height="45px" style="position: relative; margin: 10px;">
                                                <img name="{{institution.name}}" width="45px" height="45px" class="img-circle pointer {{if institution.selected 'selected' 'not-selected'}}" src="/static/img/institutions/shields-rounded-corners/{{institution.id}}-shield-rounded-corners.png" alt="{{institution.name}}" title="{{institution.name}}">
                                            </div>
                                        </a>
                                    </td>
                                {{/each}}
                            </tr>
                        </table>
                    </div>
                {{/if}}
                {{#if more}}
                    <div class="text-muted pointer" onclick={{action 'toggleMore'}}>
                        {{fa-icon "caret-down"}}{{t 'new_project.more'}}
                    </div>
                    <div class="form-group m-v-sm">
                        <label for="&quot;projectDesc" class="f-w-lg text-bigger">{{t 'general.description'}}</label>
                        {{input placeholder=(t 'new_project.description_placeholder') class='form-control noresize project-desc' value=description type="text"}}
                    </div>
                    <div class="form-group m-v-md">
                        <label for="&quot;projectTemplate" class="f-w-lg text-bigger">{{t 'new_project.template_title'}}</label>
                        <p class="f-w-xs help-text">{{t 'new_project.template_search_help'}}</p>
                        {{#power-select
                            options=nodesAvailable
                            selected=templateFrom
                            noMatchesMessage=(t 'new_project.no_matches')
                            searchMessage=(t 'new_project.template_placeholder')
                            placeholder=(t 'new_project.template_placeholder')
                            onchange=(action (mut templateFrom))
                            searchField='title'
                            renderInPlace=true
                            as |node|
                        }}
                            {{node.title}}
                        {{/power-select}}
                    </div>
                {{else}}
                    <div class="text-muted pointer" onclick={{action 'toggleMore'}}>
                        {{fa-icon "caret-right"}}{{t 'new_project.more'}}
                    </div>
                {{/if}}
            </div>
        {{/modal.body}}
        {{#modal.footer}}
            {{#bs-button onClick=(action 'closeModal') type='default'}}{{t 'general.cancel'}}{{/bs-button}}
            <button onclick={{action 'createNode'}} class="btn btn-success {{unless nodeTitle.length 'disabled'}}">{{t 'general.create'}}</button>
        {{/modal.footer}}
    {{else}}
        {{#modal.body}}
            {{! template-lint-disable bare-strings }}
            <button data-dismiss="modal" aria-label="Close" class="close" onclick={{action 'closeModal'}}><span aria-hidden="true">×</span></button>
            {{! template-lint-enable bare-strings}}
            <h4 class="add-project-success text-success">{{t 'new_project.success_message'}}</h4>
        {{/modal.body}}
        {{#modal.footer}}
            {{#bs-button onClick=(action 'reload') type='default'}}{{t 'new_project.stay_here'}}{{/bs-button}}
            <a href="/{{done}}/" class="btn btn-success">{{t 'new_project.go_to_new'}}</a>
        {{/modal.footer}}
    {{/if}}
{{/bs-modal}}