<h4 class="list-group-item-heading">
    <span class="component-overflow f-w-lg" style="line-height: 1.5;">
        {{#unless node.public}}
            <span>
                {{fa-icon 'lock'}}
                {{#bs-tooltip}}{{t 'node_blurb.private_tooltip'}}{{/bs-tooltip}}
            </span>
        {{/unless}}
        {{node-blurb/node-icon category=node.category}}
        <a href='/{{node.id}}/'>{{node.title}}</a>
        | {{t blurbTypeTitle}} {{date}}
    </span>
    {{#if (eq blurbType 'fork')}}
        <div class="pull-right generic-dropdown">
            {{#bs-dropdown as |dd|}}
                {{#dd.button}}<span class="glyphicon glyphicon-option-horizontal"></span>{{/dd.button}}
                {{#dd.menu as |m|}}
                    {{#m.item}}
                        <a href="/{{node.id}}/contributors/">{{t 'node_blurb.fork.manage_contributors'}}</a>
                    {{/m.item}}
                    {{#m.item}}
                        <a href="/{{node.id}}/settings/">{{t 'general.settings'}}</a>
                    {{/m.item}}
                    {{#m.item}}
                        {{! template-lint-disable invalid-interactive }}
                        <a onclick={{action 'delete' node}} role='button'>{{t 'general.delete'}}</a>
                        {{! template-lint-enable invalid-interactive }}
                    {{/m.item}}
                {{/dd.menu}}
            {{/bs-dropdown}}
        </div>
    {{/if}}
</h4>
<div class="project-authors">
    {{contributor-list contributors=authors useLink=true nodeId=node.id}}
</div>
<span class="text-muted">{{description}}</span>
