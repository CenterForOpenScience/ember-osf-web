<OsfDialog
    @isOpen={{@isOpen}}
    @onClose={{@closeDialog}}
    as |dialog|
>
    <dialog.heading>
        {{t 'osf-components.files-widget.create_folder'}}
    </dialog.heading>
    <dialog.main local-class='dialog-main'>
        <FormControls
            @changeset={{@changeset}}
            as |form|
        >
            <form.text
                @placeholder={{t 'osf-components.files-widget.folder_name_placeholder'}}
                @valuePath='name'
            />
        </FormControls>
    </dialog.main>
    <dialog.footer>
        <BsButton
            @type='default'
            @onClick={{dialog.close}}
        >
            {{t 'general.cancel'}}
        </BsButton>
        <BsButton
            disabled={{@shouldDisableButtons}}
            @type='primary'
            @onClick={{action @createFolder (hash onSuccess=dialog.close)}}
        >
            {{t 'osf-components.files-widget.create'}}
        </BsButton>
    </dialog.footer>
</OsfDialog>
