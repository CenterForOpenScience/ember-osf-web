<a
    href={{this._href}}
    target={{this.target}}
    rel={{this.rel}}
    class={{if this.isActive 'active'}}
    onclick={{action this._onclick}}
    ...attributes
>
    {{yield (hash
        active=this.isActive
    )}}
</a>
