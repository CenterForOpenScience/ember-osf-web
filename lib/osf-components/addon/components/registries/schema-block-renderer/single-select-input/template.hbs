<FormControls
    @changeset={{@changeset}}
    @disabled={{@disabled}}
    @shouldShowMessages={{@shouldShowMessages}}
    as |form|
>
    <form.select
        data-test-single-select-input
        @id={{@schemaBlock.registrationResponseKey}}
        @valuePath={{@schemaBlock.registrationResponseKey}}
        @options={{@optionBlocks}}
        as |option|
    >
        {{option.displayText}}
    </form.select>
</FormControls>