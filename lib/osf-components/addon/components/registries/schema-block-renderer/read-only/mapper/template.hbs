{{yield (hash
    page-heading=(
        component
        'registries/schema-block-renderer/read-only/page-heading'
    )
    section-heading=(
        component
        'registries/schema-block-renderer/read-only/section-heading'
    )
    subsection-heading=(
        component
        'registries/schema-block-renderer/read-only/subsection-heading'
    )
    paragraph=(
        component
        'registries/schema-block-renderer/read-only/paragraph'
    )
    question-label=(
        component
        'registries/schema-block-renderer/read-only/label'
    )
    short-text-input=(
        component
        'registries/schema-block-renderer/read-only/base'
    )
    long-text-input=(
        component
        'registries/schema-block-renderer/read-only/base'
    )
    single-select-input=(
        component
        'registries/schema-block-renderer/read-only/base'
    )
    multi-select-input=(
        component
        'registries/schema-block-renderer/read-only/base'
    )
    contributors-input=(
        component
        'registries/schema-block-renderer/read-only/read-only-contributor-list'
    )
)}}