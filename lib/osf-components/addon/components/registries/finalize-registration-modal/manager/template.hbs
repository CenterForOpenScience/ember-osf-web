{{yield (hash
    setEmbargoEndDate=(action this.setEmbargoEndDate)
    setCreateDoi=(action this.setCreateDoi)
    submitRegistration=(perform this.submitRegistration)
    registration=this.registration
    isOpen=this.isOpen
    renderInPlace=this.renderInPlace
    hasEmbargoEndDate=this.hasEmbargoEndDate
)}}
