{{yield (hash
    currentPageManager=this.currentPageManager
    isValid=this.isValid
    lastPage=this.lastPage
    pageManagers=this.pageManagers
    onInput=(perform this.onInput)
    submitDraftRegistration=(perform this.submitRegistration)
)}}
