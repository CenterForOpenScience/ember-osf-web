{{yield (hash
    selectedNodes=this.selectedNodes
    rootNode=this.rootNode
    loadAllChildNodes=this.loadAllChildNodes
    nodesIncludingRoot=this.nodesIncludingRoot
    selectAll=(action this.selectAll)
    clearAll=(action this.clearAll)
    addNode=(action this.addNode)
    removeNode=(action this.removeNode)
    onChange=(action this.onChange)
    isChecked=(action this.isChecked)
    isOpen=this.isOpen
)}}