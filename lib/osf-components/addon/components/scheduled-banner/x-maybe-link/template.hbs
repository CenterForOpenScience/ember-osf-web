{{#if @href}}
    <OsfLink
        target='_blank'
        rel='noopener'
        @href={{@href}}
        @onClick={{action 'click' 'link' (concat 'Banner - ' @banner.name) target=this.analytics}}
    >
        {{yield}}
    </OsfLink>
{{else}}
    {{yield}}
{{/if}}

