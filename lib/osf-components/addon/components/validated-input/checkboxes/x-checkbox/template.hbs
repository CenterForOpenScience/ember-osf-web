<Input
    aria-label={{this.ariaLabel}}
    @type='checkbox'
    @checked={{this.checked}}
    @id={{this.checkboxId}}
    @name={{this.checkboxName}}
    @disabled={{this.disabled}}
    @change={{action @onCheck @option}}
/>