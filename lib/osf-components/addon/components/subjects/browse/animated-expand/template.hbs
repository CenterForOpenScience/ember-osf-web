<AnimatedContainer local-class='ExpandyContainer'>
    <AnimatedIf
        @predicate={{@isExpanded}}
        @use={{this.expandListTransition}}
        @duration=200
    >
        {{yield}}
    </AnimatedIf>
</AnimatedContainer>
