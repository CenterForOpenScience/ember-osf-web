{{#if this.slides}}
    <BsCarousel
        data-analytics-scope='Institution Carousel'
        local-class='institution-carousel-container'
        @interval={{0}}
        @tabindex={{0}}
        @showIndicators={{false}}
        @prevControlLabel={{t 'general.previous'}}
        @nextControlLabel={{t 'general.next'}}
        @showControls={{this.showControls}}
        as |carousel|
    >
        {{#each this.slides as |slide|}}
            <carousel.slide>
                {{#each slide as |institution|}}
                    <div
                        local-class='institution'
                        role='option'
                        data-test-institution-carousel-item='{{institution.id}}'
                    >
                        <OsfLink
                            data-analytics-name='visit_institution'
                            @href={{institution.links.html}}
                        >
                            <img
                                class='img-circle img-responsive'
                                name={{institution.name}}
                                logopath={{institution.logoRoundedUrl}}
                                src={{institution.logoRoundedUrl}}
                                alt='{{institution.name}}'
                                title='{{institution.name}}'
                            >
                        </OsfLink>
                    </div>
                {{/each}}
            </carousel.slide>
        {{/each}}
    </BsCarousel>
{{/if}}
