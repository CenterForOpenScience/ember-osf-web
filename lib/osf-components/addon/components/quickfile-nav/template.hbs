<div class='container'>
    {{#if @user}}
        <h3>
            <Link
                @route='guid-user.quickfiles'
                @models={{array @user.id}}
            >
                {{t 'quickfiles.title' user-name=@user.fullName}}
            </Link>
        </h3>
    {{/if}}
</div>
