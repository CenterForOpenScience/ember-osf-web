<PowerSelect
    @options={{@options}}
    @search={{@search}}
    @afterOptionsComponent={{component
        'after-options'
        hasMore=@hasMore
        loadMore=@loadMore
        isLoading=@loadMore.isRunning
    }}
>

</PowerSelect>