{{#docs-demo as |demo|}}
    {{#demo.example name='tags-widget.editable.hbs'}}
        {{tags-widget
            tags=tags
            addTag=(action addTag)
            removeTag=(action removeTag)
            readOnly=readOnly
            showAdd=true
            analyticsScope='Handbook Tags'
        }}

    {{/demo.example}}

    {{#bs-button class='center-block m-b-lg' onClick=(action (mut readOnly) (not readOnly))}}
        set: readOnly = {{not readOnly}}
    {{/bs-button}}

    {{demo.snippet 'tags-widget.editable.hbs'}}
{{/docs-demo}}