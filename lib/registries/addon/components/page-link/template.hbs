{{yield (hash
     validationClass=this.pageClass
     models=(array this.draftId this.pageIndexWithSlug)
     pageIsActive=this.pageIsActive
     icon=this.pageIcon
     indexWithSlug=this.pageIndexWithSlug
)}}