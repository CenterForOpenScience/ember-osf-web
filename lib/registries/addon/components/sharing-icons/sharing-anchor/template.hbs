{{#if this.href}}
    <OsfLink
        data-analytics-name={{@analyticsName}}
        @onClick={{@closeDropdown}}
        @href={{this.href}}
        target='_blank'
        rel='noopener'
    >
        <FaIcon @icon={{@icon}} @fixedWidth={{true}} />
        {{#if @showText}}
            {{@medium}}
        {{/if}}
    </OsfLink>

{{/if}}
