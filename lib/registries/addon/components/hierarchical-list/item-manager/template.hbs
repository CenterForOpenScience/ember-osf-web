{{yield (hash
    item=this.item
    children=this.children
    toggleShowChildren=(action this.toggleShowChildren)
    isShowChildren=this.isShowChildren
    listManager=@listManager
    onChange=(action this.onChange)
    itemChecked=this.itemChecked
    isRoot=this.isRoot
)}}