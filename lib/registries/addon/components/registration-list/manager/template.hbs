{{yield (hash
    filterParams=this.filterParams
    filterState=@filterState
    provider=@provider
    reloadRegistrationsList=(action (mut this.reloadRegistrationsList))
    sortRegistrationsBy=this.sortRegistrationsBy
    sort=this.sort
)}}
