<ContentPlaceholders as |placeholder|>
    <PaginatedList::HasMany
        @model={{@manager.provider}}
        @relationshipName='registrations'
        @bindReload={{action (mut @manager.reloadSubmissionsList)}}
        @query={{@manager.filterParams}}
        as |list|
    >
        <list.item as |registration|>
            {{#if registration}}
                <RegistrationList::Card
                    @registration={{registration}}
                    @filterState={{@manager.filterState}}
                />
            {{else}}
                {{placeholder.text lines=1}}
            {{/if}}
        </list.item>
        <list.empty>
            {{t 'registries.registrationList.noRegistrationsFound' filterState=@manager.filterState}}
        </list.empty>
    </PaginatedList::HasMany>
</ContentPlaceholders>
