{{title (t 'registries.application.page_title') replace=true}}

{{#osf-header as |header|}}
    {{#header.navbar activeService=activeService as |ctx|}}
        {{#ctx.links as |links|}}
            {{!-- {{links.quickfiles}} --}}
            {{!-- {{links.myprojects}} --}}
            {{links.search route=searchRoute}}
            {{links.support route=supportRoute}}
            {{links.donate}}
        {{/ctx.links}}
    {{/header.navbar}}
{{/osf-header}}

{{outlet}}
