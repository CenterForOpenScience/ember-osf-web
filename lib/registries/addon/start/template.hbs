{{#if this.loading}}
    <div local-class='ContentBackground Loading'>
        <LoadingIndicator @dark={{true}} />
    </div>
{{else}}
    {{title (t 'registries.start.page_title')}}
    <div local-class=''>
        <div class='container'>
            <div class='row'>
                <h1>{{t 'registries.start.page_title'}}</h1>
                {{!-- This is where the registries start page will be. --}}
            </div>
        </div>
    </div>
{{/if}}
