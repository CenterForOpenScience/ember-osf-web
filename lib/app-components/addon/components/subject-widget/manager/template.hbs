{{yield (hash
    subjects=this.subjects
    add=(action this.add)
    remove=(action this.remove)
    discardChanges=(action this.discardChanges)
    save=(perform this.save)
    querySubjects=(perform this.querySubjects)
    selectSubject=(action this.selectSubject)
    queryResults=this.queryResults
    rootSubjects=this.rootSubjects
    isSaving=this.isSaving
    isLoading=this.isLoading
    hasChanged=this.hasChanged
    editMode=this.editMode
    loadingNodeSubjects=this.loadingNodeSubjects
    loadingRootSubjects=this.loadingRootSubjects
    selectedSubject=this.selectedSubject
    provider=this.provider
)}}
