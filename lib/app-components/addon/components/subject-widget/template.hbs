<SubjectWidget::Manager
    @model={{@model}}
    @editMode={{true}}
    as |subjectManager|
>
    <SubjectWidget::Display @manager={{subjectManager}} />
    <SubjectWidget::Browser @manager={{subjectManager}} />
</SubjectWidget::Manager>
