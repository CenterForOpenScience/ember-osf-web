{{#if useExternalLink}}
    <a href="{{provider.domain}}">
        <img src="{{logoAsset}}" alt="{{provider.name}}">
    </a>
{{else}}
    {{#link-to 'provider.discover' provider.id}}
        <img src="{{logoAsset}}" alt="{{provider.name}}">
    {{/link-to}}
{{/if}}
