import Base from '../base/component';
import styles from './styles';
import layout from './template';

export default class ActiveFilterCollectedType extends Base {
    layout = layout;
    styles = styles;
}
